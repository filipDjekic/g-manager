name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      environment:
        description: Protected deployment environment
        required: true
        type: choice
        options: [staging, production]
        default: staging

concurrency:
  group: release-${{ inputs.environment || 'production' }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: write

jobs:
  images:
    name: Build, SBOM and image policy
    runs-on: ubuntu-24.04
    outputs:
      backend: ${{ steps.images.outputs.backend }}
      frontend: ${{ steps.images.outputs.frontend }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Authenticate to GHCR
        run: echo "$GITHUB_TOKEN" | docker login ghcr.io -u "$GITHUB_ACTOR" --password-stdin
        env: { GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}' }
      - name: Build and publish immutable images with provenance and SBOM
        env:
          REGISTRY: ghcr.io/${{ github.repository_owner }}
        run: |
          docker buildx build gm --file gm/Dockerfile --build-arg APP_VERSION="$GITHUB_SHA" --tag "$REGISTRY/gmanager-backend:$GITHUB_SHA" --provenance=mode=max --sbom=true --push
          docker buildx build frontend/g-manager --file frontend/g-manager/Dockerfile --build-arg APP_VERSION="$GITHUB_SHA" --tag "$REGISTRY/gmanager-frontend:$GITHUB_SHA" --provenance=mode=max --sbom=true --push
      - id: images
        name: Resolve registry digests
        env:
          REGISTRY: ghcr.io/${{ github.repository_owner }}
        run: |
          backend_digest="sha256:$(docker buildx imagetools inspect "$REGISTRY/gmanager-backend:$GITHUB_SHA" --raw | sha256sum | cut -d' ' -f1)"
          frontend_digest="sha256:$(docker buildx imagetools inspect "$REGISTRY/gmanager-frontend:$GITHUB_SHA" --raw | sha256sum | cut -d' ' -f1)"
          echo "backend=$REGISTRY/gmanager-backend@$backend_digest" >> "$GITHUB_OUTPUT"
          echo "frontend=$REGISTRY/gmanager-frontend@$frontend_digest" >> "$GITHUB_OUTPUT"
      - name: Reject unapproved high or critical image vulnerabilities
        run: |
          docker run --rm aquasec/trivy:0.59.1 image --ignore-unfixed --severity HIGH,CRITICAL --exit-code 1 '${{ steps.images.outputs.backend }}'
          docker run --rm aquasec/trivy:0.59.1 image --ignore-unfixed --severity HIGH,CRITICAL --exit-code 1 '${{ steps.images.outputs.frontend }}'

  deploy:
    name: Deploy and prove rollback
    needs: images
    runs-on: [self-hosted, gmanager-deploy]
    environment: ${{ inputs.environment || 'production' }}
    timeout-minutes: 45
    env:
      BACKEND_IMAGE: ${{ needs.images.outputs.backend }}
      FRONTEND_IMAGE: ${{ needs.images.outputs.frontend }}
      APP_RELEASE: ${{ github.sha }}
      DEPLOY_ENV: ${{ inputs.environment || 'production' }}
      CANONICAL_ORIGIN: ${{ vars.CANONICAL_ORIGIN }}
      DOCUMENT_STORAGE_BACKEND: s3
      DOCUMENT_S3_ENDPOINT: ${{ vars.DOCUMENT_S3_ENDPOINT }}
      DOCUMENT_S3_REGION: ${{ vars.DOCUMENT_S3_REGION }}
      DOCUMENT_S3_BUCKET: ${{ vars.DOCUMENT_S3_BUCKET }}
      MIGRATION_DB_PASSWORD: ${{ secrets.MIGRATION_DB_PASSWORD }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Materialize runner-only secrets
        working-directory: ops/deploy
        env:
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_ROOT_PASSWORD: ${{ secrets.DB_ROOT_PASSWORD }}
          S3_ACCESS_KEY: ${{ secrets.DOCUMENT_S3_ACCESS_KEY }}
          S3_SECRET_KEY: ${{ secrets.DOCUMENT_S3_SECRET_KEY }}
          BACKUP_KEY: ${{ secrets.BACKUP_ENCRYPTION_KEY }}
        run: |
          install -d -m 0700 secrets
          printf %s "$JWT_SECRET" > secrets/jwt_secret
          printf %s "$DB_USERNAME" > secrets/db_username
          printf %s "$DB_PASSWORD" > secrets/db_password
          printf %s "$DB_ROOT_PASSWORD" > secrets/db_root_password
          printf %s "$S3_ACCESS_KEY" > secrets/document_s3_access_key
          printf %s "$S3_SECRET_KEY" > secrets/document_s3_secret_key
          printf %s "$BACKUP_KEY" > secrets/backup_encryption_key
          chmod 0600 secrets/*
      - name: Deploy through migration, restore and smoke gates
        run: ./ops/deploy/deploy.sh
      - name: Staging rollback rehearsal and candidate redeploy
        if: env.DEPLOY_ENV == 'staging'
        run: |
          test -f ops/deploy/releases/previous.env
          ./ops/deploy/rollback.sh
          ./ops/deploy/deploy.sh
      - name: Staging load smoke
        if: env.DEPLOY_ENV == 'staging'
        env:
          BASE_URL: ${{ vars.CANONICAL_ORIGIN }}
          CUSTOMER_EMAIL: ${{ secrets.SMOKE_CUSTOMER_EMAIL }}
          CUSTOMER_PASSWORD: ${{ secrets.SMOKE_CUSTOMER_PASSWORD }}
          MANAGER_EMAIL: ${{ secrets.SMOKE_MANAGER_EMAIL }}
          MANAGER_PASSWORD: ${{ secrets.SMOKE_MANAGER_PASSWORD }}
        run: docker run --rm -v "$PWD/performance/k6:/scripts:ro" -e BASE_URL -e CUSTOMER_EMAIL -e CUSTOMER_PASSWORD -e MANAGER_EMAIL -e MANAGER_PASSWORD grafana/k6:0.57.0 run /scripts/critical-flows.js
      - name: Passive DAST baseline
        if: env.DEPLOY_ENV == 'staging'
        run: docker run --rm zaproxy/zap-stable:2.16.1 zap-baseline.py -t "$CANONICAL_ORIGIN" -m 3
      - name: Remove materialized secrets
        if: always()
        run: find ops/deploy/secrets -type f -delete
