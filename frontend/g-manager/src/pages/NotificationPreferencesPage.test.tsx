import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { describe,expect,it,vi } from 'vitest'; import { notificationApi } from '../api/notificationApi'; import { NotificationPreferencesPage } from './NotificationPreferencesPage'
vi.mock('../api/notificationApi',()=>({notificationApi:{preferences:vi.fn(),savePreference:vi.fn()}}))
describe('NotificationPreferencesPage',()=>{it('makes mandatory security channels explicit and non-disableable',async()=>{vi.mocked(notificationApi.preferences).mockResolvedValue([{type:'SECURITY_SESSION_STARTED',mandatory:true,inAppEnabled:true,emailEnabled:true}]);render(<MemoryRouter><NotificationPreferencesPage/></MemoryRouter>);expect(await screen.findByText(/Obavezno bezbednosno/)).toBeVisible();screen.getAllByRole('checkbox').forEach(value=>expect(value).toBeDisabled())})})
