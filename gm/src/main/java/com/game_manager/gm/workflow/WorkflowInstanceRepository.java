package com.game_manager.gm.workflow; import jakarta.persistence.LockModeType; import java.util.*; import org.springframework.data.jpa.repository.*; import org.springframework.data.repository.query.Param;
public interface WorkflowInstanceRepository extends JpaRepository<WorkflowInstance,UUID>{@Lock(LockModeType.PESSIMISTIC_WRITE)@Query("select i from WorkflowInstance i join fetch i.definitionVersion v join fetch v.definition where i.id=:id")Optional<WorkflowInstance> locked(@Param("id")UUID id);@Query("select i from WorkflowInstance i join fetch i.definitionVersion v join fetch v.definition where i.id=:id")Optional<WorkflowInstance> detail(@Param("id")UUID id);List<WorkflowInstance> findByRequesterIdOrderByCreatedAtDesc(UUID requester);long countByStatus(WorkflowStatus status);long countByStatusAndDueAtBefore(WorkflowStatus status,java.time.Instant now);}
